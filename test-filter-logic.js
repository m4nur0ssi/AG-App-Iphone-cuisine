const fs = require('fs');

// Simple script to test the logic
const { mockRecipes } = require('./src/data/mockData.js'); // Wait, it's a TS file

// Just a dummy file. I will use the actual code to read mockData.ts and test.
